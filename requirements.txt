# Pinned to versions verified working on Python 3.14 (win_amd64).
# ---- Core RAG orchestration ----
langchain==1.3.11
langchain-community==0.4.2
langchain-groq==1.1.3

# ---- LLM (Groq API) ----
groq==0.37.1

# ---- Embeddings (BGE via sentence-transformers; pulls torch + transformers) ----
sentence-transformers==5.6.0

# ---- Vector store ----
chromadb==1.5.9

# ---- Document ingestion & parsing ----
beautifulsoup4==4.15.0
playwright==1.60.0
requests==2.34.2
pymupdf==1.27.2.3
pdfplumber==0.11.10

# ---- User interface ----
streamlit==1.58.0

# ---- Config / utilities ----
python-dotenv==1.2.2

# ---- Testing / evaluation (Phase 8; optional — the suite also runs standalone) ----
pytest==8.4.2
